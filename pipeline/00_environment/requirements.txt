# ============================================================
# VMS Deposit Discovery AI — Python Pipeline Dependencies
# Bathurst Mining Camp, New Brunswick
# Updated: 2026-04-28 — compatible with Python 3.14 on Windows
# ============================================================
# Install: pip install -r requirements.txt
#
# NOTE: Python 3.14 on Windows — using pyogrio instead of fiona
# (fiona requires a pre-built GDAL which is not yet available for Py3.14)

# ── Spatial & Raster ────────────────────────────────────────
geopandas>=1.0.0
rasterio>=1.3.10
pyproj>=3.6.1
shapely>=2.0.4
pyogrio>=0.9.0             # Replaces fiona as GeoPackage I/O backend

# ── Data Handling ────────────────────────────────────────────
pandas>=2.1.0
numpy>=1.26.4
pyarrow>=15.0.0            # For parquet I/O
requests>=2.31.0
tqdm>=4.66.0

# ── Machine Learning ─────────────────────────────────────────
scikit-learn>=1.4.0
xgboost>=2.0.3
imbalanced-learn>=0.12.0   # SMOTE oversampling

# ── Hyperparameter Tuning ────────────────────────────────────
optuna>=3.6.0

# ── Explainability ───────────────────────────────────────────
shap>=0.45.0

# ── Visualisation ────────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0

# ── Notebooks ────────────────────────────────────────────────
jupyter>=1.0.0
ipykernel>=6.29.0

# ── Utilities ────────────────────────────────────────────────
joblib>=1.4.0              # Model serialisation
python-dotenv>=1.0.1       # Environment variable management
